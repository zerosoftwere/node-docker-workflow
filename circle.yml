machine:
  services:
    - docker

dependencies:
  override:
    - pip install --upgrade docker-compose

test:
  override:
    - docker-compose run -d --no-deps web npm test

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY